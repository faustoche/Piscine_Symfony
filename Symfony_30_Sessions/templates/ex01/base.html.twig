<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{% block title %}My Skyblog{% endblock %}</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
  
  <style>
    body{
      margin:0;
      font-family: Tahoma, Arial, sans-serif;
      background: url('https://www.transparenttextures.com/patterns/asfalt-dark.png');
      color:#fff;
      cursor:url('https://cur.cursors-4u.net/cursors/cur-9/cur817.cur'), auto;
    }
    header{
      background:linear-gradient(to right, #ff00aa, #ff66ff);
      text-align:center;
      padding:1rem;
      border-bottom:3px solid #000;
      animation: flashbg 1s infinite alternate;
    }
    @keyframes flashbg{
      from{background-color:#ff00aa}
      to{background-color:#ff33cc}
    }
    header h1{
      font-size:2.5rem;
      margin:0;
      color:yellow;
      text-shadow:2px 2px 0 #000, 0 0 10px #fff;
      font-family: "Comic Sans MS", cursive;
      text-align:center;
    }
    .container{
      display:grid;
      grid-template-columns:250px 1fr 250px;
      gap:10px;
      max-width:1200px;
      margin:0 auto;
      padding:1rem;
    }
    .sidebar{
      background:#000;
      padding:1rem;
      border:3px dashed #00ff00;
      font-size:0.9rem;
      color:#0f0;
      text-align:center;
    }
    .sidebar h2{
      background:#00ff00;
      color:#000;
      font-size:1.1rem;
      margin-top:0;
      padding:0.2rem 0.5rem;
      font-family:"Comic Sans MS", cursive;
      text-align:center;
    }
    .content{
      background:#111;
      padding:1rem;
      border:5px groove #ff00aa;
    }
    .post{
      margin-bottom:2rem;
      background:#222;
      border:4px double #ff00aa;
      padding:1rem;
      box-shadow:0 0 15px #ff00aa;
    }
    .post img{
      max-width:100%;
      display:block;
      margin-bottom:0.5rem;
      border:5px ridge #00ff00;
    }
    .post-title{
      font-size:1.5rem;
      margin:0 0 0.5rem;
      color:#ff00aa;
      text-shadow:1px 1px 0 #000;
      font-family:"Comic Sans MS", cursive;
      text-align:center;
    }
    .tags{
      color:#00ff00;
      font-size:0.85rem;
    }
    marquee{
      color:#ff0;
      font-weight:bold;
      background:#000;
      padding:0.3rem;
      border-top:2px solid #ff00aa;
      border-bottom:2px solid #ff00aa;
    }
    a {
      color:#0ff;
      text-decoration:underline;
    }
    
    /* --- CSS FORMULAIRES & BOUTONS --- */
    input, textarea, select {
        background: #000;
        color: #0f0;
        border: 2px solid #0f0;
        padding: 5px;
        width: 100%;
        box-sizing: border-box;
        margin-bottom: 10px;
        font-family: "Comic Sans MS", cursive;
    }
    label {
        color: #ff00aa;
        font-weight: bold;
        display: block;
        text-align: left;
    }
    button, .btn {
        background: #ff00aa;
        color: #fff;
        border: 3px double #fff;
        padding: 5px 10px;
        font-family: "Comic Sans MS", cursive;
        cursor: pointer;
        text-decoration: none;
        display: inline-block;
        font-size: 1rem;
        margin: 5px;
        text-align: center;
    }
    button:hover, .btn:hover {
        background: #fff;
        color: #ff00aa;
        border-color: #ff00aa;
    }
    .alert {
        border: 2px dashed red;
        background: #300;
        color: red;
        padding: 10px;
        margin-bottom: 15px;
        font-weight: bold;
    }

    /* --- CSS ADMIN --- */
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th { border-bottom: 2px solid #ff00aa; color: yellow; padding: 5px; text-align: left; }
    td { border-bottom: 1px dashed #333; padding: 5px; color: #fff; }
  </style>
</head>
<body>
  <header>
    <h1>★ My SNooPY SkYYBlôG ★</h1>
  </header>
  
  <marquee scrollamount="10">Welcome to my blog !!! Leave a comment ;) ★ x33333 <3333333333333333333 </marquee>
  
  <div class="container">
    
    {# --- LEFT SIDEBAR (PROFILE) --- #}
    <aside class="sidebar">
      <h2>Profile</h2>
      <p><img src="https://media.giphy.com/media/aeu60CPZd8zw4/giphy.gif" alt="Avatar Snoopy" style="width:100%;border:4px inset #ff00aa"></p>
      
      {% if app.user %}
          <p style="color: yellow; font-weight: bold;">{{ app.user.username }}</p>
          <p>Rep: {{ app.user.reputation }} pts</p>
          <p>
              <a href="{{ path('app_post_new') }}">+ New Post</a><br>
              <a href="{{ path('app_logout') }}">Logout</a>
          </p>
          {% if is_granted('ROLE_ADMIN') %}
            <p><a href="{{ path('app_admin_index') }}" style="color: red;">ADMIN AREA</a></p>
          {% endif %}
      {% else %}
          <p>Welcome, guest!</p>
          <p>
            <a href="{{ path('app_login') }}">Log in</a><br>
            <a href="{{ path('app_register') }}">Register</a>
          </p>
      {% endif %}
      
      <br>
      <p>Description : I love Snoopy !</p>
    </aside>

    {# --- CENTER CONTENT --- #}
    <main class="content">
        {% block body %}{% endblock %}
    </main>

    {# --- RIGHT SIDEBAR (INFOS) --- #}
    <aside class="sidebar">
      <h2>Infos</h2>
      <p>Created : Today</p>
      <p>Visits : 7 000 000+</p>
      <h2>Tags</h2>
      <p class="tags">cool thanks xoxoooo snoopy<br>my friend<br>super cute</p>
    </aside>
    
  </div>
</body>
</html>